import { TemplateConfig } from "../../types/template"

const templateConfig: TemplateConfig = {
    id: 'modern',
    name: 'Modern RAG UI',
    category: 'saas',
    description: 'Clean and modern RAG UI for SaaS products',
    version: '1.0.0'
}

export default templateConfig
